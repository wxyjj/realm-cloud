server:
  port: 8200

spring:
  application:
    name: auth
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

feign:
  client:
    config:
      default:
        connectTimeout: 60000
        readTimeout: 60000